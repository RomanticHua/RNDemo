
(1) PropTypes 找不到 .
	npm install --save prop-types 安装
	import PropTypes from 'prop-types'; 导入

(2)	Command `run-android` unrecognized. Make sure that you have run `npm install` and that
	you are inside a react-native project.
	
	使用命令:  yarn add react-native-cli 

(3) undefined is not a function 
	
	方法名写错了.
(4) React Native unable to load script from assets index.android.bundle on windows

    设置IP和端口, 例如模拟器设置:localhost:8081,如果是真机,则设置电脑的ip地址加上8081端口.
